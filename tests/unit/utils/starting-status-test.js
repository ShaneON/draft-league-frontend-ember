import startingStatus from 'draft-league-frontend-ember/utils/starting-status';
import { module, test } from 'qunit';

module('Unit | Utility | starting status');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = startingStatus();
  assert.ok(result);
});
